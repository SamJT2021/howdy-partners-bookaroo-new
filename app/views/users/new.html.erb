<div class="sign-up-background">

<%= render "shared/navigation_bar_logged_out_sign_up_page" %>

  <div class="credentials-container">
    <%= image_tag "icon.png", class:"logo-icon"%>
    <h1>Sign up</h1>
    <%= form_for @user do |f|%>
    <div class="form-section" id="form-username">
      <div><%= f.label :username%></div>
      <% if !@user.errors[:username].empty? %>
      <div class="error-message"><%= @user.errors[:username].first %></div>
      <% end %>
      <%= f.text_field :username, class:"sign-up-input"%>
    </div>
    <div class="form-section" id="form-password"> 
     <div> <%= f.label :password%></div>
      <% if !@user.errors[:password].empty? %>
        <p class="error-message"><%= @user.errors[:password].first %></p>
      <% end %>
      <%= f.password_field :password, class:"sign-up-input"%>
    </div>
    <div class="form-section" id="form-profile-picture">
      <%= f.label :profile_picture_url %><br>
      <%= f.text_area :profile_picture, class:"profile-picture-input"%><br>
    </div> 

    <%= f.submit 'Sign up', class: "button-type-3"%>
    <%end%>
  </div>
</div>
